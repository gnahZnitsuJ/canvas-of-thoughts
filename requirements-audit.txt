# Optional, deterministic code-audit tooling. Keep separate from the runtime
# dependency set because requirements.txt participates in model compatibility.
coverage==7.15.4
complexipy==7.0.1
radon==6.0.1
